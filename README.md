# People First Network V3

A relationship management application for remembering people, interactions, follow-ups, handwritten cards, meeting preparation, events, and bulk contact imports.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

Cloudflare build output directory: `dist`.

See `UPLOAD-FIRST.md` before uploading the project to GitHub.
